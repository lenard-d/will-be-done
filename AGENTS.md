# Agent Notes

## HyperDB Terminology

- Action: a function defined with `action()`.
- Selector: a function defined with `selector()`.
- Query: a `selectFrom` query.
- Mutation: an `insert`, `upsert`, or `delete` operation.
